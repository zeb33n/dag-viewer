[project]
name = "dag-viewer"
dynamic = ["version"]
dependencies = [
    "zensical>=0.0.46",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"


[tool.setuptools]
py-modules = ["dag_viewer"]
